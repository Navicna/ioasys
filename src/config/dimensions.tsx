import { Dimensions } from "react-native";

export const { height, width } = Dimensions.get('screen');

export const CARD_WIDTH = width - 34;

export const CARD_HEIGHT = height * 0.14;

export const SPACING = 16;